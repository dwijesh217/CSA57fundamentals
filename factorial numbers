#include<stdio.h>
int main() 
{
	int i,n,fact=1;
	
	printf("Enter a number : ");
	scanf("%d",&n);
	
	for(i=1;i<=n;i++)
	{
	 fact = fact*i;
			}
		
 printf("\nFactorial of %d is %d",n,fact);
	 	
return 0;	
}
  output:
  Enter a number : 10

Factorial of 10 is 3628800
